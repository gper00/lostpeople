---
export const prerender = false;

import DashboardLayout from '@/layouts/DashboardLayout.astro';
import ImageUpload from '@/components/dashboard/ImageUpload.astro';
import { connectDB } from '@/lib/db';
import User from '@/models/User';

const user = Astro.locals.user;
if (!user) {
  return Astro.redirect('/login');
}

await connectDB();
const profile = await User.findById(user.id)
  .select('fullname username bio image socialMedia')
  .lean();

const social = (profile?.socialMedia ?? {}) as unknown as Record<string, string | null>;
const smFb = social.facebook ?? '';
const smIg = social.instagram ?? '';
const smTw = social.twitter ?? '';
const smWa = social.whatsapp ?? '';
const smTg = social.telegram ?? '';
---

<DashboardLayout title="Account | Lostpeople Dashboard">
  <div class="max-w-xl">
    <div class="mb-8">
      <h1 class="font-display text-2xl text-ink">Account</h1>
      <p class="text-sm text-ink-muted italic mt-1">{user.email}</p>
    </div>

    <!-- Profile -->
    <section class="border border-dust rounded-lg p-5 mb-5">
      <h2 class="font-display text-base text-ink mb-1">Profile</h2>
      <p class="text-sm text-ink-muted italic mb-5">
        Update your public profile information.
      </p>

      <form id="profile-form" class="flex flex-col gap-5">
        <div>
          <label for="fullname" class="font-ui text-xs text-ink-muted block mb-1">Full name</label>
          <input type="text" id="fullname" value={profile?.fullname ?? ''} required minlength="5" maxlength="100"
            class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-base rounded-lg focus:outline-none focus:border-accent transition-colors" />
        </div>

        <div>
          <label for="username" class="font-ui text-xs text-ink-muted block mb-1">Username</label>
          <input type="text" id="username" value={profile?.username ?? ''} maxlength="25" pattern="[a-zA-Z0-9_-]+"
            class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-base rounded-lg focus:outline-none focus:border-accent transition-colors" />
          <p class="text-xs text-ink-muted mt-1">Letters, numbers, hyphens, underscores. Leave empty to remove.</p>
        </div>

        <div>
          <label for="bio" class="font-ui text-xs text-ink-muted block mb-1">Bio</label>
          <textarea id="bio" maxlength="400" rows="3"
            class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-base rounded-lg focus:outline-none focus:border-accent transition-colors resize-y">{(profile?.bio as string) ?? ''}</textarea>
        </div>

        <ImageUpload name="image" value={profile?.image} label="Avatar" />

        <div>
          <span class="font-ui text-xs text-ink-muted block mb-2">Social media links</span>
          <div class="flex flex-col gap-2">
            <div class="flex items-center gap-2">
              <span class="text-xs text-ink-muted w-20 shrink-0">Facebook</span>
              <input type="url" id="sm-facebook" value={smFb} maxlength="150" placeholder="https://facebook.com/..."
                class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-sm rounded-lg focus:outline-none focus:border-accent transition-colors" />
            </div>
            <div class="flex items-center gap-2">
              <span class="text-xs text-ink-muted w-20 shrink-0">Instagram</span>
              <input type="url" id="sm-instagram" value={smIg} maxlength="150" placeholder="https://instagram.com/..."
                class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-sm rounded-lg focus:outline-none focus:border-accent transition-colors" />
            </div>
            <div class="flex items-center gap-2">
              <span class="text-xs text-ink-muted w-20 shrink-0">Twitter / X</span>
              <input type="url" id="sm-twitter" value={smTw} maxlength="150" placeholder="https://twitter.com/..."
                class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-sm rounded-lg focus:outline-none focus:border-accent transition-colors" />
            </div>
            <div class="flex items-center gap-2">
              <span class="text-xs text-ink-muted w-20 shrink-0">WhatsApp</span>
              <input type="url" id="sm-whatsapp" value={smWa} maxlength="150" placeholder="https://wa.me/..."
                class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-sm rounded-lg focus:outline-none focus:border-accent transition-colors" />
            </div>
            <div class="flex items-center gap-2">
              <span class="text-xs text-ink-muted w-20 shrink-0">Telegram</span>
              <input type="url" id="sm-telegram" value={smTg} maxlength="150" placeholder="https://t.me/..."
                class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-sm rounded-lg focus:outline-none focus:border-accent transition-colors" />
            </div>
          </div>
        </div>

        <div>
          <button type="submit" id="profile-submit" class="lp-btn-primary">
            Save profile
          </button>
        </div>
      </form>
    </section>

    <section class="border border-dust rounded-lg p-5">
      <h2 class="font-display text-base text-ink mb-1">Change password</h2>
      <p class="text-sm text-ink-muted italic mb-5">
        Enter your current password, then choose a new one. This signs you out of other devices.
      </p>

      <form id="password-form" class="flex flex-col gap-5">
        <div>
          <label for="current" class="font-ui text-xs text-ink-muted block mb-1">Current password</label>
          <input type="password" id="current" required autocomplete="current-password"
            class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-base rounded-lg focus:outline-none focus:border-accent transition-colors" />
        </div>
        <div>
          <label for="next" class="font-ui text-xs text-ink-muted block mb-1">New password</label>
          <input type="password" id="next" required minlength="8" autocomplete="new-password"
            class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-base rounded-lg focus:outline-none focus:border-accent transition-colors" />
        </div>
        <div>
          <label for="confirm" class="font-ui text-xs text-ink-muted block mb-1">Confirm new password</label>
          <input type="password" id="confirm" required minlength="8" autocomplete="new-password"
            class="w-full px-3 py-2 border border-dust bg-parchment text-ink-body text-base rounded-lg focus:outline-none focus:border-accent transition-colors" />
        </div>

        <div>
          <button type="submit" id="pw-submit" class="lp-btn-primary">
            Update password
          </button>
        </div>
      </form>
    </section>
  </div>
</DashboardLayout>

<script>
  import { authClient } from '@/lib/auth-client';
  import { toast } from '@/lib/ui';

  const form = document.getElementById('password-form') as HTMLFormElement;
  const submitBtn = document.getElementById('pw-submit') as HTMLButtonElement;

  // Profile form
  const profileForm = document.getElementById('profile-form') as HTMLFormElement;
  const profileSubmit = document.getElementById('profile-submit') as HTMLButtonElement;

  const getVal = (id: string) => (document.getElementById(id) as HTMLInputElement | null)?.value ?? '';

  profileForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    profileSubmit.textContent = 'Saving...';
    profileSubmit.disabled = true;

    try {
      const res = await fetch('/api/user/profile', {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          fullname: getVal('fullname'),
          username: getVal('username'),
          bio: getVal('bio'),
          image: getVal('image'),
          socialMedia: {
            facebook: getVal('sm-facebook'),
            instagram: getVal('sm-instagram'),
            twitter: getVal('sm-twitter'),
            whatsapp: getVal('sm-whatsapp'),
            telegram: getVal('sm-telegram'),
          },
        }),
      });
      const data = await res.json();
      if (!res.ok) throw new Error(data.error || 'Failed to save profile');
      toast('Profile saved', 'success');
    } catch (err: any) {
      toast(err.message || 'Failed to save profile', 'error');
    } finally {
      profileSubmit.textContent = 'Save profile';
      profileSubmit.disabled = false;
    }
  });

  // Password form
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const currentPassword = (document.getElementById('current') as HTMLInputElement).value;
    const newPassword = (document.getElementById('next') as HTMLInputElement).value;
    const confirm = (document.getElementById('confirm') as HTMLInputElement).value;

    if (newPassword.length < 8) {
      toast('New password must be at least 8 characters', 'error');
      return;
    }
    if (newPassword !== confirm) {
      toast('New passwords do not match', 'error');
      return;
    }

    submitBtn.textContent = 'Updating...';
    submitBtn.disabled = true;

    const { error } = await authClient.changePassword({
      currentPassword,
      newPassword,
      revokeOtherSessions: true,
    });

    submitBtn.textContent = 'Update password';
    submitBtn.disabled = false;

    if (error) {
      toast(error.message || 'Failed to change password', 'error');
      return;
    }
    form.reset();
    toast('Password updated', 'success');
  });
</script>
