---
import ListLayout from '@/layouts/ListLayout.astro';
import PostItem from '@/components/blog/PostItem.astro';
import { connectDB } from '@/lib/db';
import Post from '@/models/Post';
import '@/models/User';

await connectDB();
const { tag: decodedTag } = Astro.params;

const posts = await Post.find({ status: 'published', tags: decodedTag })
  .sort({ createdAt: -1 })
  .populate('userId', 'fullname username image')
  .lean();
---

<ListLayout
  title={`#${decodedTag} | Lostpeople`}
  description={`Posts tagged with ${decodedTag}`}
  keywords={decodedTag}
>
  <div class="lp-tag-header">
    <a href="/tags" class="lp-back">← Tags</a>
    <div class="lp-tag-header__row">
      <h1 class="lp-tag-header__name">{decodedTag}</h1>
      <span class="lp-tag-header__count">{posts.length} post{posts.length !== 1 ? 's' : ''}</span>
    </div>
  </div>

  {posts.length > 0 ? (
    <div class="lp-post-list">
      {posts.map((post) => <PostItem post={post} />)}
    </div>
  ) : (
    <p class="text-sm text-ink-muted italic">No posts with this tag.</p>
  )}
</ListLayout>
