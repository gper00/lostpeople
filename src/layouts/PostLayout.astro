---
import BaseLayout from './BaseLayout.astro';
import Sidebar from '@/components/shared/Sidebar.astro';
import ReadingProgress from '@/components/shared/ReadingProgress.astro';

interface Props {
  title: string;
  description?: string;
  image?: string;
  url?: string;
}

const { title, description, image, url } = Astro.props;
---

<BaseLayout {title} {description} {image} {url}>
  <ReadingProgress />
  <div class="lp-container">
    <div class="lp-layout">
      <main class="lp-main">
        <slot />
      </main>
      <Sidebar />
    </div>
  </div>
</BaseLayout>
